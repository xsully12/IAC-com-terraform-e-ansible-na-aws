- hosts: terraform-ansible
  tasks:
  - name: criando o arquivo
    copy:
      dest: /home/ubuntu/index.html
      content: <h1>Consegui, finalmente</h1>
  - name: Criando o servidor
    shell: "nohup busybox httpd -f -p 8080 &"